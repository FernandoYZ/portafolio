---
// Hero Section - Ultra ligero
---
<section class="min-h-screen flex items-center justify-center px-6 pt-20 pb-32 md:pb-20">
  <div class="max-w-3xl mx-auto text-center">
    <!-- Availability badge -->
    <div class="inline-flex items-center gap-2 px-3 py-1.5 mb-8 rounded-full glass-light">
      <span class="w-2 h-2 rounded-full bg-green-500"></span>
      <span class="text-xs text-text-tertiary">Disponible para proyectos</span>
    </div>

    <!-- Main heading -->
    <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-semibold tracking-tight mb-6">
      <span class="text-gradient">Fernando Yovera</span>
    </h1>

    <!-- Role -->
    <p class="text-xl sm:text-2xl md:text-3xl text-text-tertiary font-light tracking-tight mb-6">
      Ingeniero de Sistemas
    </p>

    <!-- Description -->
    <p class="text-base sm:text-lg text-text-muted max-w-xl mx-auto mb-12 leading-relaxed">
      Diseño e implemento sistemas escalables y observables.
      Especializado en arquitecturas distribuidas y backend con Go.
    </p>

    <!-- CTAs -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
      <a
        href="/#projects"
        class="w-full sm:w-auto px-8 py-4 bg-text-primary text-bg-primary font-medium rounded-2xl hover:bg-text-secondary inline-flex items-center justify-center gap-3"
      >
        Ver proyectos
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </a>
      <a
        href="/#contact"
        class="w-full sm:w-auto px-8 py-4 glass rounded-2xl font-medium text-text-primary hover:bg-accent-dim inline-flex items-center justify-center gap-3"
      >
        Contactar
      </a>
    </div>
  </div>
</section>
